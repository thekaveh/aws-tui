"""Single source of truth for the aws-tui package version.

Updating this constant is the only place version bumps need to happen;
hatchling reads it via the `[tool.hatch.version]` config in pyproject.toml.
"""

__version__ = "0.4.0"
